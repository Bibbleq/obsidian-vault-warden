{
  "name": "vault-warden",
  "version": "0.3.1",
  "description": "Schema-driven, write-time vault hygiene for Obsidian. Validates the active note's frontmatter against YAML rule manifests stored in your vault.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "vitest run",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [],
  "author": "Ben Lee",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "builtin-modules": "^5.3.0",
    "esbuild": "^0.28.1",
    "obsidian": "latest",
    "tslib": "^2.8.1",
    "typescript": "~5.9.3",
    "vitest": "^4.1.10"
  }
}
